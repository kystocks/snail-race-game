/**
 * Shared constants for JScargot
 * These values should match the backend constants
 */

// Valid snail colors - must match backend SNAIL_COLORS
export const SNAIL_COLORS = ['red', 'blue', 'yellow', 'green', 'orange', 'purple'];

// Track length for the race board
export const TRACK_LENGTH = 9; // start(0) + 7 spaces + finish(8)
